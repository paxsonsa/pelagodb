query friends_of_friends {
  seed(func: uid(Person:4_0)) {
    name
L   -[KNOWS]-># @filter(age {
      name
      age
    }
   seed(func: uid(Person:4_0)) {
    name
L   -[KNOWS]->#) {
      name
      age
    }
  }
}
