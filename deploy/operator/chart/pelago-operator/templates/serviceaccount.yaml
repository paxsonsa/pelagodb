{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pelago-operator.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "pelago-operator.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
